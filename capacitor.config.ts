import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mrinal.deliveryApp',
  appName: 'DeliveryApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
